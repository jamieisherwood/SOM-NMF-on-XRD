Clustering X-ray diffraction images through using Self-organizing maps. 
One appraoch uses SOM directly on the compressed data.
The other reduces the dimension of the data using NMF, and then uses SOM to cluster the images.
